import java.util.Scanner;
public class Amelia {
    Boolean Good = true;
    Scanner scanner = new Scanner(System.in);
    public void Run() {
        while(Good) {
            String nextLine = scanner.nextLine();
                if(nextLine.equalsIgnoreCase("Hi")) {
                System.out.println("Great");
            }
        }
    }
}

